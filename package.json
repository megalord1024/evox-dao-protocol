{
  "name": "hardhat-project",
  "scripts": {
    "test": "npx hardhat test ./test/test.js",
    "compile": "npx hardhat compile",
    "init": "npx hardhat run ./deploy/evoxdaoinit.ts --network amoy",
    "token_deploy": "npx hardhat run ./deploy/evoxtoken.ts --network amoy",
    "timelock_deploy": "npx hardhat run ./deploy/timelock.ts --network amoy",
    "sablier_deploy": "npx hardhat run ./deploy/evoxsablier.ts --network amoy",
    "governor_deploy": "npx hardhat run ./deploy/evoxgovernor.ts --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "@sablier/v2-core": "^1.1.2",
    "dotenv": "^16.4.5",
    "hardhat-deploy": "^0.12.4"
  }
}
